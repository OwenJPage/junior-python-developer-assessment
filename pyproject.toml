[project]
name = "uni-job-assessment"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "psycopg[binary]==3.3.3",
]

[dependency-groups]
api = [
    "fastapi[standard]>=0.135.1",
]
dev = [
    "ruff>=0.15",
]